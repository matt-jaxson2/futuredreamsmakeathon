{
  "name": "future-dreams",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite src --host;",
    "build": "vite build src --emptyOutDir; cp -a src/admin/ _build/admin/",
    "prod": "yarn build; vite preview src --host"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
